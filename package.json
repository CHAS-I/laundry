{
  "name": "laundry",
  "version": "1.0.0",
  "private": "true",
  "packageManager": "npm@10.9.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.4.4"
  }
}